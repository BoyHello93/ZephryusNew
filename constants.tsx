import React from 'react';

// Default initial code if nothing is generated (fallback)
export const DEFAULT_CODE = `<!-- Welcome to Zephyr Mentor -->
<!-- Write your code here -->
<div id="app">
  <h1>Hello World</h1>
</div>

<style>
  body { font-family: sans-serif; padding: 20px; }
  h1 { color: #4f46e5; }
</style>

<script>
  console.log("Ready to code!");
</script>
`;

export const MOCK_COURSE_DATA = {
  // Can be used for testing UI without API calls if needed
};